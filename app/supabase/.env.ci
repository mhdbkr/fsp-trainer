# Secrets FACTICES pour la CI (tests à signatures locales). Les vrais vivent dans supabase/.env (ignoré).
STRIPE_SECRET_KEY=sk_test_dummy_ci
STRIPE_WEBHOOK_SECRET=whsec_test_local_secret
# Fonction `ai` : fournisseur factice (aucune vraie clé en CI ni en local de test).
AI_CHAIN_BRIEF=mock:brief
AI_CHAIN_CHAT=mock:chat
AI_ALLOW_MOCK=1
